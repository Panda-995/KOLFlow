services:
  kolflow:
    image: ghcr.io/panda-995/kolflow:1.4.3
    container_name: kolflow
    ports:
      - "3441:3000"
    volumes:
      # {{ if and .DATA_PATH (index .DATA_PATH 0) }}
      - type: bind
        source: |-
          ${DATA_PATH}
        target: /app/data
      # {{ else }}
      - ./data:/app/data
      # {{ end }}
    environment:
      TZ: Asia/Shanghai
      NODE_ENV: production
      PORT: 3000
      DATA_DIR: /app/data
      JWT_SECRET: ${JWT_SECRET}
      INVITE_CODE: ${INVITE_CODE}
      CORS_ORIGIN: "*"
    restart: always
    healthcheck:
      test:
        - CMD
        - node
        - -e
        - require('http').get('http://localhost:3000/api/health',r=>process.exit(r.statusCode===200?0:1)).on('error',()=>process.exit(1))
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 15s
