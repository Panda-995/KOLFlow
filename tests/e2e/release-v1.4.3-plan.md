# v1.4.3 发布验证计划

按 e2e-test-orchestrator 的验证流程复用既有业务与浏览器用例，并增加目录配置及持久化检查。

| ID | 检查 | 预期 |
| --- | --- | --- |
| COPY-01 | 读取最终两架构 UPK 参数 | 中英文说明正确，邀请码规则不变，文件夹可选且可修改 |
| PATH-01 | Go 模板实际渲染并解析 YAML | 缺少参数、空列表、空字符串均保留原目录；自定义目录及特殊字符准确挂载；每种情况仅一个挂载 |
| CORE-01 | 运行既有 22 个接口、业务及周期通知回归 | 全部通过 |
| E2E-01 | 执行 3 个 Playwright 用例 | 备份导入、模板、核心页面和 PC/移动素材通过 |
| UPGRADE-01 | 以 v1.4.2 模块建立 12 表历史数据，原地升级后复制到新路径加载 | 所有数据、结构保留，完整性及外键正常 |
| NATIVE-01 | amd64 / arm64 原生运行旧镜像种子数据，再以新镜像启动、重启及复制自选目录 | 原账号、历史商单、表结构与记录一致，目录可写，重启持久化正常 |
| RELEASE-01 | 构建、下载并核验 Docker、两份 UPK、APK、素材 ZIP | 版本、架构、签名、图标素材及 SHA256 正确 |

测试使用合成数据和隔离目录。没有连接 NAS 或 Android 真机，不将真机安装或交互列为通过。
